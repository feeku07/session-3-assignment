# Robot Framework — Task 3 (part 2)

## Setup
```powershell
pip install robotframework robotframework-seleniumlibrary
```

## Run tests
```powershell
robot saucedemo.robot
```

Reports auto-generate: `log.html`, `report.html`, `output.xml`

## Coverage
- Login scenario reimplemented from Selenium (Python) in Robot Framework's keyword-driven syntax
- See `../selenium/NOTES.md` for a comparison between Selenium and Robot Framework